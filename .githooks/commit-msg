#!/bin/sh
# Strip Cursor agent attribution from commit messages (IDE/cloud agents).
grep -v '^Co-authored-by: Cursor ' "$1" \
  | grep -v 'cursoragent@cursor.com' \
  | grep -v '^Made-with: Cursor' > "$1.tmp" && mv "$1.tmp" "$1"
